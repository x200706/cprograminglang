#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
   int arr[5] = {0};
   for (int i = 1; i <= 5; ++i) {
      num = "%10d", 1 + (rand() % 39;
      printf());
      if (i % 5 == 0) {
         puts("");
      } 
   } 
} 

// 二分搜尋那題用fig 6.19改成遞迴就ok了